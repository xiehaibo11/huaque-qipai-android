package com.nanbeiyule.game.gameplay;

/**
 * Kong kinds of the Wave 2-B command/event contract. The wire names are the
 * enum names verbatim; {@code EXPOSED} is the original 直杠, {@code CONCEALED}
 * the 暗杠 and {@code FILL} the 补杠.
 */
public enum GameplayKongType {
    EXPOSED,
    CONCEALED,
    FILL
}
