package com.nanbeiyule.game;

/** Reports shop time-membership page visibility and horizontal scroll to effect overlays. */
interface ShopMembershipPageListener {
    void onMembershipPageChanged(boolean visible, float scrollOffset);
}
