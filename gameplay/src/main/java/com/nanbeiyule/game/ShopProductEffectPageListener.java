package com.nanbeiyule.game;

/** Reports category visibility and vertical scrolling to the original product effect layer. */
interface ShopProductEffectPageListener {
    void onProductEffectPageChanged(ShopCategory category, float verticalScrollOffset);
}
